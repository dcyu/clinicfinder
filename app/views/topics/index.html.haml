%h1 Listing topics

%ul
  - @topics.each do |topic|
    %li
      %span= topic.name
      %span= link_to 'Show', topic
      %span= link_to 'Edit', edit_topic_path(topic)
      %span= link_to 'Destroy', topic, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Topic', new_topic_path
